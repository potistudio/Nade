# Nade — Built to Create. Programmed to Kill Adobe.

## プロジェクト概要

『Nade』は、RustとIcedで実装された、高パフォーマンスなノンリニア動画編集アプリケーションです。
60fps以上での滑らかなプレビュー再生と、堅牢な並行処理モデルを最優先事項としています。

## 技術スタック

- 基本言語: Rust (最新のStable)
- GUIフレームワーク: Iced (Elmアーキテクチャ採用, 最新のTask / Stream APIを使用)
- 非同期ランタイム: tokio
- メッセージング: crossbeam-channel (Core -> UI), iced::Subscription
- コアロジック: nade_core (独立した映像処理クレート)

## ディレクトリ構成と責務

コードベースは「関心の分離 (Separation of Concerns)」に基づいて厳格に構造化されています。

```text
crates/nade/src/
├── app.rs               # アプリケーションのルート。状態管理とルーティングのみを担当。
├── main.rs              # エントリーポイント。
├── message.rs           # アプリケーション全体で共有される Message 定義。
├── theme.rs             # デザイン定数、カラーパレット。ハードコード禁止。
├── services/            # インフラ層。
│   └── core_service.rs  # Coreとの通信ブリッジ (後述のパターンを厳守)。
└── panels/              # UIコンポーネント層。各パネルは独立したモジュール。
    ├── mod.rs
    ├── preview.rs       # プレビュー画面描画
    ├── timeline.rs      # タイムライン描画
    └── ...
```

## 設計哲学

Nadeは、**「妥協のないパフォーマンス」と「Rustらしい安全性」**を両立させるノンリニア動画編集システムです。
以下の3つの柱を絶対的な指針とします。

- Zero-Latency Response: UIスレッドは常にノンブロッキングであり、ユーザー入力に対して即座に応答しなければならない。
- Safe Concurrency: 並行処理における競合状態や未定義動作を、言語機能と設計パターンによってコンパイル時に排除する。
- Strict Isolation: 描画(UI)、通信(Bridge)、計算(Core)の責務を明確に分離し、相互依存による複雑化を防ぐとともに、拡張性を確保し、進化を止めない。

## アーキテクチャパターン

Nadeは、「非同期UI / 同期Coreハイブリッドアーキテクチャ」を採用しています。
このアーキテクチャは、以下の3つのレイヤーを組み合わせることで構築されています。

### Layer 1: 非同期UI

- 役割: 状態の表示とユーザー入力の受付。
- 特性: async/awaitベースのイベント駆動。
- 制約: 計算負荷の高い処理を一切行ってはならない。すべての重い処理はコマンドとして発行される。

### Layer 2: ブリッジ

- 役割: 異なる並行モデル(非同期 vs 同期)間のインピーダンスミスマッチを解消する緩衝地帯。
- 特性:
    - イベント駆動: ポーリングを行わず、シグナル駆動で動作することでCPUリソースを浪費しない。
    - 安全な受け渡し: スレッド間のデータ受け渡しにおいて、所有権システムとスマートポインタ(ArcやMutex等)を活用し、論理的な安全性を保証する。
- 原則: ブリッジは橋渡しの役割に徹する。ロジックを持たず、データの運搬と型の変換のみに責任を持つ。

### Layer 3: 計算ロジック

- 役割: フレームのリアルタイムレンダリング、ステート管理、ビジネスロジック。
- 特性: CPUバウンドな同期処理。
- 相互作用: UIから完全に分離されている。計算結果をチャネルに流すことだけに集中する。

## 堅牢性の基準

### 並行処理とリソース

- ロックの粒度: Mutexのようなロックの使用は「リソースの所有権を移動または共有する一瞬」に限定する。ロングロック(処理中のロック保持)はシステム全体のストールを招くため禁止する。
- 不変性: スレッド間で共有するデータ(特に巨大な画像データ)は原則として不変(Immutable)とし、Arcによる参照カウント方式でコストを最小化する。

### コンポーネント設計

- Stateless Views: UIコンポーネント（パネル等）は内部状態を極力持たず、渡された Model を純粋関数的に描画する「プロジェクション」として実装する。
- Type-Driven Messages: コンポーネント間の通信には、文字列や汎用型ではなく、目的ごとに定義された厳密な enum 型を使用する。これにより、意図しないメッセージによる誤動作を防ぐ。

### Error Handling & Safety

- No Unsafe: unsafe ブロックの使用は、FFI（外部関数インターフェース）など回避不可能な場合を除き禁止する。パフォーマンス最適化を理由とした安易な unsafe は技術的負債となる。
- Fail-Fast in Initialization: 起動時や初期化フェーズでの矛盾（リソース不足、設定ミス）は unwrap 等で即座に停止させる。ただし、実行時（Run-loop内）では Result を用いて適切にリカバリまたはログ出力を行う。

## 実装ガイドライン & ルール

### Core-UI通信ブリッジ (Strict Rule)

Core（ロジック）と UI（Iced）の通信は、以下の **「Core Bridge パターン」** を厳守してください。

### ロックフリーな受信ループ

受信ループ内での Mutex ロックは禁止です。
recv_timeout によるポーリングは禁止です（CPU負荷削減のため）。完全なブロッキング待機 (recv()) を使用します。

### 安全性

unsafeは原則禁止です。
`Arc<Mutex<Receiver>>`を使用し、ストリーム構築時の **「最初の一回だけ」** ロックしてReceiverをクローンする手法を採用してください。

```rust
// 正しい実装パターン (services/core_service.rs)
pub fn build_stream(conn: &CoreConnection) -> BoxStream<'static, Message> {
    // 1. 初期化時のみロックしてハンドルを複製
    let rx = conn.0.lock().unwrap().clone();

    iced::stream::channel(100, move |mut output| async move {
        loop {
            // 2. ループ内はロックフリー & ブロッキング待機
            let msg = tokio::task::spawn_blocking({
                let rx = rx.clone();
                move || rx.recv() // timeoutなし
            }).await;
            
            // ... 送信処理 ...
        }
    }).boxed()
}
```

### UI / パネル実装
God Object の回避:

NadeApp (app.rs) に描画ロジックを書かないでください。必ず panels/*.rs に委譲します。

ステートレスなビュー:

パネルの view 関数には、&Model 全体ではなく、必要なデータのみ（例: &PreviewState）を渡してください。これにより単体テストと再利用性を高めます。

スタイリング:

色やサイズは theme.rs で定義された定数を使用してください。マジックナンバー/カラーは避けてください。

C. パフォーマンス
UIスレッドのブロック禁止: 重い処理はすべて tokio::task::spawn または Core 側へオフロードしてください。

クローンの最小化: 画像データ（フレームバッファ）などの巨大なデータは、可能な限り Arc や Bytes を活用し、ディープコピーを回避してください。

5. 開発の指針
リファクタリング: コードの複雑化を検知したら、即座にモジュール分割を提案してください。

型安全性: unwrap() はプロトタイプ以外では避け、適切なエラーハンドリングを行ってください（ただし、Core Bridgeの初期化ロックなど、論理的にパニックすべき箇所は除く）。