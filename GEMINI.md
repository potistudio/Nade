# Nade — Built to Create. Programmed to Kill Adobe.

## プロジェクト概要

『Nade』は、RustとIcedで実装された、高パフォーマンスなノンリニア動画編集アプリケーションです。
60fps以上での滑らかなプレビュー再生と、堅牢な並行処理モデルを最優先事項としています。

## 技術スタック

- 基本言語: Rust (最新のStable)
- GUIフレームワーク: Iced (Elmアーキテクチャ採用, 最新のTask / Stream APIを使用)
- 非同期ランタイム: tokio
- メッセージング: crossbeam-channel (Core -> UI), iced::Subscription
- コアロジック: nade_core (独立した映像処理クレート)

## ディレクトリ構成と責務

コードベースは「関心の分離 (Separation of Concerns)」に基づいて厳格に構造化されています。

```text
crates/nade/src/
├── app.rs               # アプリケーションのルート。状態管理とルーティングのみを担当。
├── main.rs              # エントリーポイント。
├── message.rs           # アプリケーション全体で共有される Message 定義。
├── theme.rs             # デザイン定数、カラーパレット。ハードコード禁止。
├── services/            # インフラ層。
│   └── core_service.rs  # Coreとの通信ブリッジ (後述のパターンを厳守)。
└── panels/              # UIコンポーネント層。各パネルは独立したモジュール。
    ├── mod.rs
    ├── preview.rs       # プレビュー画面描画
    ├── timeline.rs      # タイムライン描画
    └── ...
```

## 設計哲学

Nadeは、**「妥協のないパフォーマンス」と「Rustらしい安全性」**を両立させるノンリニア動画編集システムです。
以下の3つの柱を絶対的な指針とします。

- Zero-Latency Response: UIスレッドは常にノンブロッキングであり、ユーザー入力に対して即座に応答しなければならない。
- Safe Concurrency: 並行処理における競合状態や未定義動作を、言語機能と設計パターンによってコンパイル時に排除する。
- Strict Isolation: 描画(UI)、通信(Bridge)、計算(Core)の責務を明確に分離し、相互依存による複雑化を防ぐとともに、拡張性を確保し、進化を止めない。

## アーキテクチャパターン

Nadeは、「非同期UI / 同期Coreハイブリッドアーキテクチャ」を採用しています。
このアーキテクチャは、以下の3つのレイヤーを組み合わせることで構築されています。

### Layer 1: 非同期UI

- 役割: 状態の表示とユーザー入力の受付。
- 特性: async/awaitベースのイベント駆動。
- 制約: 計算負荷の高い処理を一切行ってはならない。すべての重い処理はコマンドとして発行される。

### Layer 2: ブリッジ

- 役割: 異なる並行モデル(非同期 vs 同期)間のインピーダンスミスマッチを解消する緩衝地帯。
- 特性:
    - イベント駆動: ポーリングを行わず、シグナル駆動で動作することでCPUリソースを浪費しない。
    - 安全な受け渡し: スレッド間のデータ受け渡しにおいて、所有権システムとスマートポインタ(ArcやMutex等)を活用し、論理的な安全性を保証する。
- 原則: ブリッジは橋渡しの役割に徹する。ロジックを持たず、データの運搬と型の変換のみに責任を持つ。

### Layer 3: 計算ロジック

- 役割: フレームのリアルタイムレンダリング、ステート管理、ビジネスロジック。
- 特性: CPUバウンドな同期処理。
- 相互作用: UIから完全に分離されている。計算結果をチャネルに流すことだけに集中する。

## 堅牢性の基準

### 並行処理とリソース

- ロックの粒度: Mutexのようなロックの使用は「リソースの所有権を移動または共有する一瞬」に限定する。ロングロック(処理中のロック保持)はシステム全体のストールを招くため禁止する。
- 不変性: スレッド間で共有するデータ(特に巨大な画像データ)は原則として不変(Immutable)とし、Arcによる参照カウント方式でコストを最小化する。

### コンポーネント設計

- Stateless Views: UIコンポーネント(パネル等)は内部状態を極力持たず、渡されたModelを純粋関数的に描画する「プロジェクション」として実装する。
- Type-Driven Messages: コンポーネント間の通信には、文字列や汎用型ではなく、目的ごとに定義された厳密なenum型を使用する。これにより、意図しないメッセージによる誤動作を防ぐ。

### エラー処理と安全性

- No Unsafe: unsafeブロックの使用は、FFI(外部関数インターフェース)など回避不可能な場合を除き禁止する。パフォーマンス最適化を理由とした安易なunsafeは技術的負債となる。
- Fail-Fast in Initialization: 起動時や初期化フェーズでの矛盾(リソース不足、設定ミス等)はunwrap等で即座に停止させる。ただし、実行時(Run-loop内)ではResultを用いて適切にリカバリまたはログ出力を行う。

## 実装ガイドライン & ルール

### 分離の原則

app.rsはルーティングのみ。描画はpanels/、通信はservices/に配置する。

### 定数管理

色、サイズ、タイミング定数は、theme.rsまたは設定ファイルに集約する。

### 所有権の明確化

誰がデータを持っているかを常に意識する。安易なcloneは避け、必要な場所でのみ行う。

### Core-UI通信ブリッジ (Strict Rule)

Core(ロジック)と UI(Iced)の通信は、以下の **「Core Bridge パターン」** を厳守する。

## 開発の指針

### リファクタリング

コードの複雑化を検知したら、即座にモジュール分割を提案してください。

### 型安全性

unwrap()はプロトタイプ以外では避け、適切なエラーハンドリングを行ってください(ただし、Core Bridgeの初期化ロックなど、論理的にパニックすべき箇所は除く)。

## パフォーマンスガイドライン

### メモリ管理

映像フレームや音声バッファなどのラージバイナリは`Arc<T>`または`bytes::Bytes`で管理し、CoreからGPUメモリへの転送まで一度も`clone()`を行わない。

### 描画

タイムラインやグラフなど、要素数が動的かつ多数になるコンポーネントは、Icedの標準ウィジェットではなく`iced::widget::Canvas`を使用して描画コストを最小化する。

### 16msルール

UIスレッド(update, view)での処理は、いかなる場合もブロッキングIOを行わず、計算量はO(1)または極小のO(N)に留める。
